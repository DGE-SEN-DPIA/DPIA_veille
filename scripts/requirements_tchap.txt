# Dépendance système requise (hors pip) pour python-olm (chiffrement E2E) :
#   apt-get install -y libolm-dev
# Installée par scripts/setup_tchap.sh, qui gère aussi le contournement
# SETUPTOOLS_USE_DISTUTILS requis par atomicwrites (dépendance de matrix-nio[e2e]).
matrix-nio[e2e]~=0.25
cffi>=2.0
