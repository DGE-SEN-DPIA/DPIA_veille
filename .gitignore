# Tchap E2E crypto store
crypto_store/*
!crypto_store/.gitkeep

# Tchap bot session (contains encrypted access token)
session.txt

# Tchap runtime store (credentials + Matrix device keys)
.tchap_store/
